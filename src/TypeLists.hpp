#ifndef TYPELISTS_HPP
#define TYPELISTS_HPP

#include <boost/mpl/vector.hpp>

typedef boost::mpl::vector<int, float, double> first_list;
typedef boost::mpl::vector<int, float, double> second_list;
typedef boost::mpl::vector<int, float, double> third_list;

#endif // TYPELISTS_HPP
